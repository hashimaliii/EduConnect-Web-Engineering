server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        
        # THE MAGIC LINE: Fallback to index.html if the file isn't found
        try_files $uri $uri/ /index.html;
    }
}